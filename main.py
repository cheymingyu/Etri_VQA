import argparse
import os
import random
import shutil
import time
import warnings
import numpy as np
import logging
from tqdm import tqdm

import torch
import torch_geometric
import torch.backends.cudnn as cudnn
import pathlib

def get_args_parser():
    parser = argparse.ArgumentParser('VQA Parser', add_help=False)

#def main(args):

if __name__=="__main__":
    print("dsadad")
